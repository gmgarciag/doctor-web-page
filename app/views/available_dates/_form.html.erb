<%= form_for(available_date) do |f| %>
  <% if available_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(available_date.errors.count, "error") %> prohibited this available_date from being saved:</h2>

      <ul>
      <% available_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %>
    <%= f.text_field :day %>
  </div>

  <div class="field">
    <%= f.label :hour %>
    <%= f.number_field :hour %>
  </div>

  <div class="field">
    <%= f.label :minute %>
    <%= f.number_field :minute %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
